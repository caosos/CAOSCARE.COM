[pytest]
markers =
    real_hardware: sends real commands to real physical devices (Room 214's Home Assistant-backed Matter bulbs/AC) and leaves them in whatever state the test last set - excluded from a plain `pytest` run; opt in explicitly with `-m real_hardware` when you intend to touch the physical hardware.
addopts = -m "not real_hardware"
