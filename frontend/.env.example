# CAOS Care frontend environment example
# Copy to frontend/.env for local development or provide these at build time.
# Do not commit frontend/.env or real secrets.

# Required: base URL for the FastAPI backend without the trailing /api.
REACT_APP_BACKEND_URL=http://localhost:8000

# Optional: enable CRACO development health endpoints during local development.
ENABLE_HEALTH_CHECK=false
