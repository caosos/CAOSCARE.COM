# Wake Phrase Lab, Step 1 (text/phoneme stage). Pinned versions actually used.
wordfreq==3.1.1     # code Apache-2.0; bundled data CC-BY-SA 4.0 - used locally, never redistributed
g2p-en==2.1.0       # Apache-2.0; pulls nltk (Apache-2.0), inflect (MIT), numpy
rapidfuzz==3.14.5   # MIT
PyYAML==6.0.3       # MIT
pytest==9.1.1       # MIT (tests)
